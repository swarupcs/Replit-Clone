{
  "buildCommand": "cd ../.. && pnpm --filter @replit-clone/shared build && pnpm --filter @replit-clone/web build",
  "outputDirectory": "dist",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
