node_modules/
dist/
build/
.env
.env.*
!.env.example
# ...and the one beside it. `.env.*` is what keeps a filled-in secret out of
# the history, and an example file that ships no secrets is the exception.
!.env.expose.example
*.log
.DS_Store
.migration-src/
projects/
# Published deployment output. Real files served to the public; never source.
deployments/
.turbo/
coverage/

# Playwright
apps/web/e2e/.artifacts/
apps/web/playwright-report/
apps/web/test-results/

# Copied from packages/shared/dist by `pnpm images:prepare`, so the egress
# gateway enforces the same address policy the server does rather than a
# second copy of it.
images/egress/network.js
